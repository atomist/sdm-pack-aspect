query ScmProviderById($providerId: String!) {
  SCMProvider(providerId: $providerId) {
    url
    apiUrl
    providerType
    id
    providerId
    credential {
      scopes
      secret
    }
  }
}
