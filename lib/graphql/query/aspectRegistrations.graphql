query AspectRegistrations($state: [AspectRegistrationState], $name: [String]) {
    AspectRegistration(state: $state, name: $name, _first: 100) {
        name
        owner
        displayName
        description
        shortName
        unit
        url
        manageable
        category
        endpoint
        uuid
        state
    }
}
