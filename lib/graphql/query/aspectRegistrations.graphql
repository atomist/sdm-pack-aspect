query AspectRegistrations($state: [AspectRegistrationState], $name: [String]) {
    AspectRegistration(state: $state, name: $name) {
        name
        owner
        displayName
        description
        shortName
        unit
        url
        manageable
        category
        endpoint
        uuid
        state
    }
}
