query AspectRegistrations($enabled: [String], $name: [String]) {
    AspectRegistration(enabled: $enabled, name: $name) {
        name
        owner
        displayName
        description
        shortName
        unit
        url
        category
        endpoint
        uuid
        enabled
    }
}
